# Legacy database exports

Raw development database exports are intentionally not tracked in this public repository.

Use Laravel migrations and seeders for local setup. If demo data is needed, add synthetic fixtures that contain no personal data, credentials, payment identifiers, or production-derived records.
